{
  "name": "@glitchproof/enumerator",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:types": "vitest typecheck",
    "test:runtime": "vitest run src/tests/units/runtime",
    "test:bench": "vitest bench",
    "test:bench:compare": "vitest bench --compare",
    "test:all": "npm run test:types && npm run test:runtime"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.0.9",
    "expect-type": "^1.1.0",
    "vitest": "^4.0.17"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
